import open from "open";

export default function openUrl(siteName) {
	if (siteName === "updrop") {
		open("http://updrop.ashwincodes.com");
	}

	if (siteName === "hexhub") {
		open("http://updrop.ashwincodes.com");
	}

	if (siteName === "linkdin") {
		open("http://updrop.ashwincodes.com");
	}
}
